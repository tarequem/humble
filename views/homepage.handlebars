<ol class="post-list">
  {{#each posts as |post|}}
  <li>
    <article class="post">
      <div class="title">
<<<<<<< HEAD
        <a href="{{post.user_post}}" target="_blank">{{post.title}}</a>
        <span>({{post.user_post}})</span>
=======
        <a href="{{post.image}}" target="_blank">{{post.title}}</a>
        <img src="{{post.image}}">
>>>>>>> 72fc35d98361ef8ed23c94339bebcbd9c482229c
      </div>
      <div class="meta">
        {{post.vote_count}} point(s) by {{post.user.username}} on
        {{post.created_at}}
        |
        <a href="/post/{{post.id}}">{{post.comments.length}} comment(s)</a>
      </div>
    </article>
  </li>
  {{/each}}
</ol>

